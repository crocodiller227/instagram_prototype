<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('homepage') }}">
        <div class="logo"></div>
        PNTGRM</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            {% if app.getUser %}
            <a class="nav-item nav-link" href="{{ path('homepage') ~ "user/" ~ app.user.getId() }}">My profile</a>
            <a class="nav-item nav-link" href="{{ path('homepage') }}">Posts feed</a>
        </div>
    </div>
    <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" id="search_user_id" type="search" placeholder="Search user by username" aria-label="Search">
        <button class="btn btn-outline-info my-2 my-sm-0" id="search_user_submit" type="submit">Search</button>
    </form>
    <a href="{{ path('homepage') ~ "logout" }}" class="btn btn-danger">Logout</a>
    {% else %}
        <a class="nav-item nav-link" href="{{ path('homepage') ~ "register" }}">Register</a>
        <a class="nav-item nav-link" href="{{ path('homepage') ~ "login" }}">Authorization</a>
        </div>
        </div>
    {% endif %}
    <script src="{{ path('homepage') ~ 'js/search_user.js' }}"></script>
</nav>